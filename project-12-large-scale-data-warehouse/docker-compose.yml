version: '3'

services:

  postgres:
    image: postgres:16

    container_name: data-warehouse

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: enterprise_dw

    ports:
      - "5432:5432"
