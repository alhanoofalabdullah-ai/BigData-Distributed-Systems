print("Starting replication process...")

source_cluster = "primary-cluster"

target_cluster = "secondary-cluster"

print(f"Replicating data from {source_cluster} to {target_cluster}")

print("Replication completed successfully")
